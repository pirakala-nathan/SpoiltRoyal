%h1 Listing location_options

%table
  %thead
    %tr
      %th Name
      %th
      %th
      %th

  %tbody
    - @location_options.each do |location_option|
      %tr
        %td= location_option.name
        %td= link_to 'Show', location_option
        %td= link_to 'Edit', edit_location_option_path(location_option)
        %td= link_to 'Destroy', location_option, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Location option', new_location_option_path
